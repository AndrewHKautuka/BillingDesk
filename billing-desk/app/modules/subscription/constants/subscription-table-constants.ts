// Columns to hide on smaller screens
export const HIDDEN_ON_SMALL = new Set([
  "name",
  "category",
  "formattedCost",
  "billingCycle",
])

// Columns to hide on larger screens
export const HIDDEN_ON_LARGE = new Set(["subscription", "cylceFormattedCost"])
